{
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "cypress": "^13.12.0"
  },
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:headless": "npm run cypress run --browser chrome"
  }
}
